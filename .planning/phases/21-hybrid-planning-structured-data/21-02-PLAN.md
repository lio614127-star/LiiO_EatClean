---
phase: 21-hybrid-planning-structured-data
plan: 02
type: execute
wave: 1
depends_on: ["21-01"]
files_modified: ["LiiO_EatClean/Data/Models/FoodItemModel.swift", "LiiO_EatClean/Data/Repositories/MealRepository.swift"]
autonomous: true
requirements: ["UNIT-01", "UNIT-02", "UNIT-03"]
must_haves:
  truths:
    - "Người dùng có thể quy đổi 'chén' sang 'grams' chính xác cho cơm"
    - "Hệ thống hỗ trợ song song đơn vị Primary và Secondary"
  artifacts:
    - path: "LiiO_EatClean/Data/Models/MealComponentModel.swift"
      provides: "Swift struct for MealComponent"
---

<objective>
Xây dựng lớp Model cho Structured Meal và triển khai logic chuyển đổi đơn vị thông minh (Smart Units).
Mục tiêu: Đảm bảo độ chính xác dinh dưỡng và hỗ trợ hiển thị đơn vị dân dã.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/21-hybrid-planning-structured-data/21-CONTEXT.md
@.planning/phases/21-hybrid-planning-structured-data/21-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tạo MealComponentModel và Mapping Logic</name>
  <files>LiiO_EatClean/Data/Models/MealComponentModel.swift, LiiO_EatClean/Data/Repositories/MealRepository.swift</files>
  <action>
    1. Tạo struct `MealComponentModel` (Codable, Identifiable) tương ứng với thực thể CoreData.
    2. Cập nhật `MealFoodModel` để chứa mảng `[MealComponentModel]`.
    3. Cập nhật `MealRepository.mapMealFood` để load các components từ CoreData sang Model.
  </action>
  <verify>XCTest: Khởi tạo MealFood với 3 components và verify mapping thành công.</verify>
  <done>Dữ liệu component được load đúng vào UI Model.</done>
</task>

<task type="auto">
  <name>Task 2: Triển khai UnitConverter Service (Smart Units)</name>
  <files>LiiO_EatClean/Data/Services/UnitConverter.swift</files>
  <action>
    1. Tạo `UnitConverter` sử dụng Foundation `Measurement`.
    2. Định nghĩa `DensityMap` cho các đơn vị dân dã Việt Nam:
       - 1 chén cơm -> 200g
       - 1 bát phở -> 500g (liquids included)
       - 1 dĩa cơm tấm -> 350g
    3. Implement hàm `convert(value: Double, from: String, to: String, foodCategory: String) -> Double`.
  </action>
  <verify>Unit Test: verify "1 chén" cơm = 200g (+/- 5g).</verify>
  <done>Hệ thống quy đổi được các đơn vị phi chuẩn sang gram.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-02 | Information Disclosure | Nutrition Data | accept | Sai số 15% là chấp nhận được trong lâm sàng (Clinical Acceptable Range). |
</threat_model>

<success_criteria>
- Model hỗ trợ lưu trữ danh sách nguyên liệu.
- Logic quy đổi "chén/bát" hoạt động chính xác cho các nhóm thực phẩm VN phổ biến.
</success_criteria>

<output>
After completion, create `.planning/phases/21-hybrid-planning-structured-data/21-02-SUMMARY.md`
</output>
