---
phase: 21-hybrid-planning-structured-data
plan: 03
type: execute
wave: 2
depends_on: ["21-02"]
files_modified: ["LiiO_EatClean/Data/Repositories/FoodRepository.swift"]
autonomous: true
requirements: ["PLAN-02", "PLAN-03"]
must_haves:
  truths:
    - "Các món ăn lặp lại gần đây bị giảm thứ tự ưu tiên (Penalty)"
    - "Diversity Engine chặn không cho quá 2 món cùng nhóm Carb trong 1 ngày"
  artifacts:
    - path: "LiiO_EatClean/Data/Services/DiversityEngine.swift"
      provides: "Scoring and filtering logic for candidates"
---

<objective>
Xây dựng Diversity Engine để đảm bảo tính đa dạng và thực tế cho kế hoạch ăn uống.
Mục tiêu: Loại bỏ sự nhàm chán (plate fatigue) và ưu tiên món Việt.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/21-hybrid-planning-structured-data/21-CONTEXT.md
@.planning/phases/21-hybrid-planning-structured-data/21-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Triển khai Recency Penalty và Diversity Scoring</name>
  <files>LiiO_EatClean/Data/Services/DiversityEngine.swift</files>
  <action>
    1. Implement hàm `calculateScore(for food: FoodItem) -> Double`:
       - Base Score: 100.0
       - VietnamesePriority: +20 nếu có trong VietnameseFoods.json.
       - AvailabilityScore: +10 cho staples (cơm, cá, trứng).
       - Recency Penalty: -50 / (daysSinceLastUsed + 1).
    2. Implement hàm `filterDiverseCandidates(candidates: [FoodItem]) -> [FoodItem]`:
       - Group theo `carbBase` (Cơm, Bún, Phở, Khoai).
       - Group theo `mainIngredient` (Bò, Gà, Heo, Cá).
       - Giới hạn Max 2 món cho mỗi nhóm trong cùng một ngày.
  </action>
  <verify>Unit Test: verify một món vừa ăn ngày hôm qua có điểm thấp hơn món ăn cách đây 1 tuần.</verify>
  <done>Thuật toán Scoring và Filtering hoạt động đúng logic đã chốt.</done>
</task>

<task type="auto">
  <name>Task 2: Tích hợp Diversity Engine vào FoodRepository</name>
  <files>LiiO_EatClean/Data/Repositories/FoodRepository.swift</files>
  <action>
    Cập nhật hàm `getCandidatePool` để gọi `DiversityEngine` trước khi trả về kết quả.
    Đảm bảo pool trả về đúng kích thước slot (12-15 cho sáng, 18-25 cho trưa/tối).
  </action>
  <verify>Grep check: `getCandidatePool` gọi `DiversityEngine.calculateScore`.</verify>
  <done>FoodRepository trả về danh sách ứng viên đã được tối ưu hóa.</done>
</task>

</tasks>

<success_criteria>
- Diversity Engine lọc thành công các món lặp lại.
- Scoring ưu tiên đúng các món Việt và nguyên liệu phổ biến.
</success_criteria>

<output>
After completion, create `.planning/phases/21-hybrid-planning-structured-data/21-03-SUMMARY.md`
</output>
