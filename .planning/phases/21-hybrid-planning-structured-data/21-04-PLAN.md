---
phase: 21-hybrid-planning-structured-data
plan: 04
type: execute
wave: 3
depends_on: ["21-03"]
files_modified: ["LiiO_EatClean/Features/Meals/MealPlanViewModel.swift", "LiiO_EatClean/Data/Services/AIService.swift"]
autonomous: true
requirements: ["PLAN-01", "PLAN-04"]
must_haves:
  truths:
    - "Kế hoạch ngày được sinh ra trong < 10s"
    - "AI chỉ thực hiện 1 lần request duy nhất cho toàn bộ ngày"
  artifacts:
    - path: "LiiO_EatClean/Data/Services/HybridPlanningService.swift"
      provides: "Orchestration logic between Local and AI"
---

<objective>
Tái cấu trúc Nutrition Engine sang mô hình Hybrid Planning (Single-pass Matchmaker).
Mục tiêu: Tăng tốc độ phản hồi (<10s) và giảm chi phí Token.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/21-hybrid-planning-structured-data/21-CONTEXT.md
@.planning/phases/21-hybrid-planning-structured-data/21-AI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Local Meal Splitter</name>
  <files>LiiO_EatClean/Data/Services/HybridPlanningService.swift</files>
  <action>
    1. Tạo `HybridPlanningService`.
    2. Triển khai logic chia Calorie (Kcal Split):
       - Breakfast: 25%
       - Lunch: 35%
       - Dinner: 30%
       - Snack: 10%
    3. Hàm `preparePlanContext(targetKcal: Double)` trả về struct chứa Kcal target cho từng bữa kèm theo Candidate Pool (đã được lọc bởi DiversityEngine).
  </action>
  <verify>XCTest: Target 2000kcal -> Breakfast target 500kcal.</verify>
  <done>App tự tính toán được framework kế hoạch mà không cần AI.</done>
</task>

<task type="auto">
  <name>Task 2: Triển khai Single-pass AI Orchestration (Matchmaker Pattern)</name>
  <files>LiiO_EatClean/Data/Services/AIService.swift, LiiO_EatClean/Features/Meals/MealPlanViewModel.swift</files>
  <action>
    1. Cập nhật `AIService` để hỗ trợ prompt "Matchmaker":
       - Input: JSON chứa (Targets + Candidate Pool).
       - Output: JSON Selection (ID của món ăn đã chọn từ Pool).
    2. Tích hợp vào `MealPlanViewModel.generateDayPlan`:
       - Gọi `HybridPlanningService` lấy context.
       - Gửi 1 request duy nhất tới AI.
       - Parse kết quả và map ngược lại database.
  </action>
  <verify>Performance Test: Measure thời gian từ lúc nhấn nút đến khi có kết quả (Target < 10s).</verify>
  <done>Nutrition Engine hoạt động theo mô hình Hybrid mới.</done>
</task>

</tasks>

<success_criteria>
- AI không sinh món mới lung tung, chỉ chọn từ Candidate Pool (Matchmaker).
- Thời gian sinh kế hoạch ngày ổn định dưới 10 giây.
</success_criteria>

<output>
After completion, create `.planning/phases/21-hybrid-planning-structured-data/21-04-SUMMARY.md`
</output>
