---
phase: 21-hybrid-planning-structured-data
plan: 05
type: execute
wave: 4
depends_on: ["21-04"]
files_modified: ["LiiO_EatClean/Features/Meals/Views/MealCardView.swift", "LiiO_EatClean/Features/Meals/MealPlanViewModel.swift"]
autonomous: false
requirements: ["MEAL-02", "MEAL-03"]
must_haves:
  truths:
    - "Người dùng có thể nhấn 'Đổi món' và món mới xuất hiện tức thì (<0.3s) nếu là Local Swap"
    - "Chi tiết món ăn hiển thị rõ ràng các thành phần (MealComponents)"
  artifacts:
    - path: "LiiO_EatClean/Features/Meals/Views/StructuredMealDetailView.swift"
      provides: "UI for viewing ingredients"
---

<objective>
Nâng cấp giao diện hiển thị món ăn có cấu trúc và triển khai cơ chế Local Swap tức thì.
Mục tiêu: Cải thiện UX khi tinh chỉnh kế hoạch ăn uống.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/21-hybrid-planning-structured-data/21-CONTEXT.md
@.planning/phases/21-hybrid-planning-structured-data/21-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cập nhật MealCardView và tạo StructuredMealDetailView</name>
  <files>LiiO_EatClean/Features/Meals/Views/MealCardView.swift, LiiO_EatClean/Features/Meals/Views/StructuredMealDetailView.swift</files>
  <action>
    1. Cập nhật `MealCardView` để hiển thị tóm tắt nguyên liệu (vd: "Cơm, Cá kho, Rau muống").
    2. Tạo `StructuredMealDetailView` (Sheet) để hiển thị danh sách `MealComponent` chi tiết:
       - Tên nguyên liệu.
       - Khối lượng (grams) + Đơn vị dân dã (Secondary Unit).
       - Macro từng thành phần.
  </action>
  <verify>Checkpoint: Mở detail của một món ăn và kiểm tra danh sách nguyên liệu.</verify>
  <done>UI hiển thị được dữ liệu cấu trúc (Structured Data).</done>
</task>

<task type="auto">
  <name>Task 2: Triển khai 3-Level Swap Logic</name>
  <files>LiiO_EatClean/Features/Meals/MealPlanViewModel.swift</files>
  <action>
    1. Implement hàm `swapMeal(mealId: UUID)`:
       - Level 1 (Local): Lấy 1 món khác trong Candidate Pool cùng `substituteGroup` -> Replace tức thì (<0.3s).
       - Level 2 (Smart): Nếu Local hết, gọi AI Refresh (Single-meal swap) -> 2-5s.
       - Level 3 (Rebuild): Nếu cả 2 không ổn, cho phép user Re-generate toàn bộ bữa.
    2. Gắn logic vào nút "Swap" trên UI.
  </action>
  <verify>Performance Test: Local swap phải hoàn thành trong < 0.3s.</verify>
  <done>Tính năng đổi món hoạt động mượt mà và thông minh.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Kiểm tra cuối cùng hệ thống Hybrid Planning</name>
  <what-built>Toàn bộ engine Hybrid Planning và Structured Data Layer.</what-built>
  <how-to-verify>
    1. Chạy app, nhấn sinh kế hoạch ngày. Xác nhận tốc độ nhanh (<10s).
    2. Kiểm tra danh sách món ăn có đúng diversity không (không trùng quá nhiều gà/bò).
    3. Thử nhấn "Swap" và cảm nhận tốc độ đổi món.
    4. Xem chi tiết món ăn để verify structured ingredients.
  </how-to-verify>
  <resume-signal>Type "approved" if everything looks premium.</resume-signal>
</task>

</tasks>

<success_criteria>
- UI hiển thị chi tiết nguyên liệu một cách cao cấp (premium).
- Tính năng Swap hoạt động tức thì với các món trong Candidate Pool.
</success_criteria>

<output>
After completion, create `.planning/phases/21-hybrid-planning-structured-data/21-05-SUMMARY.md`
</output>
