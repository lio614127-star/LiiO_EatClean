---
phase: 21-hybrid-planning-structured-data
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["LiiO_EatClean/LiiO_EatClean.xcdatamodeld", "LiiO_EatClean/Data/Repositories/MealRepository.swift"]
autonomous: true
requirements: ["MEAL-01"]
must_haves:
  truths:
    - "Dữ liệu bữa ăn cũ vẫn được giữ lại sau khi cập nhật schema"
    - "Thực thể MealComponent tồn tại trong CoreData"
  artifacts:
    - path: "LiiO_EatClean/LiiO_EatClean.xcdatamodeld"
      provides: "New model version with MealComponent entity"
  key_links:
    - from: "MealFood"
      to: "MealComponent"
      via: "One-to-many relationship"
---

<objective>
Khởi tạo cấu trúc dữ liệu mới cho Structured Meal bằng cách thêm thực thể MealComponent vào CoreData.
Mục tiêu: Cho phép lưu trữ chi tiết nguyên liệu và hỗ trợ di trú dữ liệu mượt mà.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/21-hybrid-planning-structured-data/21-CONTEXT.md
@.planning/phases/21-hybrid-planning-structured-data/21-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tạo CoreData Model Version mới và thực thể MealComponent</name>
  <files>LiiO_EatClean/LiiO_EatClean.xcdatamodeld</files>
  <action>
    1. Tạo Model Version mới (v2) cho LiiO_EatClean.xcdatamodeld.
    2. Thêm thực thể `MealComponent` với các thuộc tính:
       - id: UUID
       - name: String
       - grams: Double
       - calories: Double
       - protein: Double
       - carbs: Double
       - fat: Double
       - quantity: Double
       - unit: String
       - category: String
       - cookingMethod: String
       - isOptional: Boolean
       - substituteGroup: String
    3. Thiết lập mối quan hệ:
       - `MealFood` -> `components`: To-Many (destination: MealComponent, inverse: mealFood).
       - `MealComponent` -> `mealFood`: To-One (destination: MealFood, inverse: components).
    4. Set Delete Rule của `components` là Cascade.
  </action>
  <verify>Mở .xcdatamodeld và xác nhận version mới đã được set làm Current.</verify>
  <done>Schema v2 được tạo và thực thể MealComponent được định nghĩa.</done>
</task>

<task type="auto">
  <name>Task 2: Cấu hình Lightweight Migration</name>
  <files>LiiO_EatClean/Data/Persistence/Persistence.swift</files>
  <action>
    Xác nhận `NSPersistentStoreDescription` đã được cấu hình:
    - `shouldMigrateStoreAutomatically = true`
    - `shouldInferMappingModelAutomatically = true`
    (Per RESEARCH.md)
  </action>
  <verify>Kiểm tra code khởi tạo PersistenceController.</verify>
  <done>App có thể khởi động và migrate dữ liệu mà không bị crash.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Disk -> CoreData | Dữ liệu cũ được load và transform sang schema mới. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Tampering | CoreData Migration | mitigate | Sử dụng Lightweight Migration của Apple để đảm bảo integrity. |
</threat_model>

<success_criteria>
- Thực thể MealComponent xuất hiện trong CoreData.
- Quan hệ MealFood -> MealComponent được thiết lập.
- App khởi động thành công với dữ liệu cũ.
</success_criteria>

<output>
After completion, create `.planning/phases/21-hybrid-planning-structured-data/21-01-SUMMARY.md`
</output>
