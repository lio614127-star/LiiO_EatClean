# Plan 28-03: Smart Rules & Final Integration

Objective: Implement the complex rebalance rules (Anti-Repeat, Time-Aware, etc.) and finalize the end-to-end flow.

## Proposed Changes

### Logic & Rules
- **[MODIFY] AIRebalanceService.swift**:
    - Implement `Anti-Repeat` logic (filter recently eaten meals from prompt).
    - Implement `Time-Aware` constraints (e.g., skip snacks after 20:00).
    - Implement `Vietnamese Priority` by adjusting AI prompt preferences.
    - Implement `Dry/Fresh Balance` check based on actual meal history.

### Integration & Polish
- **[MODIFY] MealPlanViewModel.swift**:
    - Add `applyRebalance(result: RebalanceResult)` to save the new plan to CoreData.
    - Add `isRebalancing` state for loading indicators.
- **[MODIFY] AppState/Notifications**:
    - Ensure smooth UI refresh after plan override.
    - Add Haptic feedback for successful rebalancing.

## Verification Plan

### Automated Tests
- `SmartRuleTests`: Verify prompt includes "Avoid" instructions for recently eaten items.
- `IntegrationTests`: Full flow from deviation detection -> AI call -> Save -> Refresh.

### Manual Verification
- Test late-night rebalance to ensure no heavy snacks are suggested.
- Test rebalance after eating a specific dish to ensure no duplicates are suggested.
