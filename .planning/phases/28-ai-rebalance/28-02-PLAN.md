# Plan 28-02: UI Components & Rebalance Preview

Objective: Implement the proactive UI triggers and the before/after comparison preview.

## Proposed Changes

### UI Components
- **[NEW] RebalanceSuggestionCard.swift**:
    - Compact card for Home tab showing deviation and CTA.
- **[NEW] RebalanceBannerView.swift**:
    - Contextual banner for Journal/Planning tab.
- **[NEW] RebalancePreviewSheet.swift**:
    - Full-height sheet for Before/After comparison.
    - Displays: `oldPlan` vs `newPlan`, `summary`, `rebalanceReason`.
- **[NEW] DiffMealCard.swift**:
    - Individual meal cards showing "Old Portion/Item" (strikethrough) vs "New Portion/Item".

### Integration
- **[MODIFY] HomeView.swift**:
    - Inject `RebalanceSuggestionCard` below the Daily Summary.
- **[MODIFY] MealsView.swift**:
    - Inject `RebalanceBannerView` at the top of the timeline.
- **[MODIFY] ProgressTabView.swift**:
    - Add "Tái cấu trúc bữa còn lại" to the secondary menu.

## Verification Plan

### Automated Tests
- `SnapshotTests`: Verify `RebalancePreviewSheet` rendering for both Portion-only and Swap scenarios.

### Manual Verification
- Trigger a rebalance from Home and confirm the Preview sheet appears with correct diffs.
