---
wave: 1
depends_on: []
files_modified:
  - LiiO_EatClean/LiiO_EatClean.xcdatamodeld
  - LiiO_EatClean/Data/Services/DailyAdherenceSnapshotService.swift
  - LiiO_EatClean/Data/Repositories/DailyPlanRepository.swift
  - LiiO_EatClean/Data/Repositories/MealRepository.swift
autonomous: true
requirements: [HEAT-01]
must_haves:
  truths:
    - "DailyAdherenceSnapshot entity exists in CoreData"
    - "SnapshotService automatically updates cache on meal/plan changes"
  artifacts:
    - path: "LiiO_EatClean/Data/Services/DailyAdherenceSnapshotService.swift"
      provides: "Event-driven adherence caching"
---

# Plan 27-01: Data Foundation

Implement the CoreData entity and the background service to manage adherence snapshots.

<task type="auto">
<read_first>
- LiiO_EatClean/LiiO_EatClean.xcdatamodeld
- LiiO_EatClean/Features/Meals/Services/MealAdherenceCalculator.swift
</read_first>
<action>
Add `DailyAdherenceSnapshot` entity to CoreData model:
- Attributes: id (UUID), date (Date), adherenceScore (Double), totalCalories (Double), totalProtein (Double), targetCalories (Double), targetProtein (Double), mealCount (Int16), plannedMealCount (Int16), dataVersion (Int16).
- Set `date` as indexed.
</action>
<verify>
<automated>
# Check if entity is present in compiled model (mock test)
</automated>
</verify>
<acceptance_criteria>
- CoreData model contains DailyAdherenceSnapshot with all 10 attributes.
</acceptance_criteria>
</task>

<task type="auto">
<read_first>
- LiiO_EatClean/Data/Repositories/DailyPlanRepository.swift
- LiiO_EatClean/Data/Repositories/MealRepository.swift
</read_first>
<action>
Update Repositories to post notifications on change:
- `DailyPlanRepository.savePlan`: Post `mealPlanDidUpdate`.
- `MealRepository.saveMeal`: (Already posts `mealLogDidUpdate`).
- `MealRepository.deleteMeal`: (Already posts `mealLogDidUpdate`).
</action>
<verify>
<automated>
grep -r "mealPlanDidUpdate" LiiO_EatClean/Data/Repositories/DailyPlanRepository.swift
</automated>
</verify>
<acceptance_criteria>
- `DailyPlanRepository` posts `mealPlanDidUpdate` notification after successful save.
</acceptance_criteria>
</task>

<task type="auto">
<read_first>
- LiiO_EatClean/Features/Meals/Services/MealAdherenceCalculator.swift
</read_first>
<action>
Create `DailyAdherenceSnapshotService.swift`:
- Initialize observers for `mealLogDidUpdate` and `mealPlanDidUpdate`.
- Implement `recalculateSnapshot(for date: Date)`:
    - Fetch meals and plan for the date.
    - Run `MealAdherenceCalculator.shared.calculate`.
    - Upsert `DailyAdherenceSnapshot` entity.
- Implement `fetchSnapshots(from: Date, to: Date)` to support bulk loading for calendar.
</action>
<verify>
<automated>
# Run DailyAdherenceSnapshotServiceTests (to be created)
</automated>
</verify>
<acceptance_criteria>
- Service correctly recalculates and persists snapshot when notified.
- `fetchSnapshots` returns data from CoreData for the requested range.
</acceptance_criteria>
</task>
