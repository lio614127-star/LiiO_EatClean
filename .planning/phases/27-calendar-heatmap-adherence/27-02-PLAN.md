---
wave: 2
depends_on: ["01"]
files_modified:
  - LiiO_EatClean/Features/Progress/ViewModels/CalendarHeatmapViewModel.swift
  - LiiO_EatClean/Features/Progress/Components/CalendarHeatmapView.swift
  - LiiO_EatClean/Features/Progress/Components/HeatmapCell.swift
autonomous: true
requirements: [HEAT-01, HEAT-02]
must_haves:
  truths:
    - "Heatmap shows 5 distinct colors based on adherence"
    - "Grid displays 7 days per row"
  artifacts:
    - path: "LiiO_EatClean/Features/Progress/Components/CalendarHeatmapView.swift"
      provides: "Grid visualization of adherence"
---

# Plan 27-02: Heatmap UI

Implement the custom calendar grid and the discrete color mapping logic.

<task type="auto">
<read_first>
- LiiO_EatClean/.planning/phases/27-calendar-heatmap-adherence/27-UI-SPEC.md
</read_first>
<action>
Create `CalendarHeatmapViewModel.swift`:
- Properties: `currentMonth: Date`, `adherenceSnapshots: [Date: DailyAdherenceSnapshot]`.
- Methods: `loadMonthData()`, `daysInMonth()`.
- Logic: Fetch snapshots from `DailyAdherenceSnapshotService`.
</action>
<verify>
<automated>
# Unit test for month generation logic
</automated>
</verify>
<acceptance_criteria>
- ViewModel correctly generates days for the current month.
- ViewModel loads snapshots for the visible range.
</acceptance_criteria>
</task>

<task type="auto">
<read_first>
- LiiO_EatClean/Features/Meals/Components/HorizontalDateStrip.swift
</read_first>
<action>
Implement `CalendarHeatmapView.swift` and `HeatmapCell.swift`:
- Use `LazyVGrid` with 7 columns.
- `HeatmapCell` styling:
    - Color mapping (D-02): Mint, Green, Yellow, Orange, Red, Gray.
    - Corner radius: 4.
- Add month navigation (Prev/Next buttons).
- Add Legend at the bottom per UI-SPEC.
</action>
<verify>
<automated>
# Screenshot verification in manual UAT
</automated>
</verify>
<acceptance_criteria>
- UI matches 27-UI-SPEC.md exactly.
- Colors correctly reflect the adherence score of each day.
- Legend is present and readable.
</acceptance_criteria>
</task>
