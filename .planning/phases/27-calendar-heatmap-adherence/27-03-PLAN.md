---
wave: 3
depends_on: ["02"]
files_modified:
  - LiiO_EatClean/Features/Progress/Components/DailyAdherenceDetailSheet.swift
  - LiiO_EatClean/Features/Progress/ProgressTabView.swift
  - LiiO_EatClean/App/ContentView.swift
  - LiiO_EatClean/Features/Meals/MealsViewModel.swift
autonomous: true
requirements: [HEAT-02, HEAT-03]
must_haves:
  truths:
    - "Tapping a heatmap cell opens the detail sheet"
    - "Deep-link navigates to the selected date in the Journal"
---

# Plan 27-03: Interaction & Deep Linking

Implement the detail sheet and the navigation flow to the Daily Journal.

<task type="auto">
<read_first>
- LiiO_EatClean/Features/Meals/MealsViewModel.swift
</read_first>
<action>
Update `MealsViewModel.swift` and `MealsView.swift`:
- Add `selectedDate` property to `MealsViewModel` (defaulting to today).
- Refactor `loadTodayMeals` to `loadMeals(for date: Date)`.
- Support setting `selectedDate` via external notification or public method.
</action>
<verify>
<automated>
# Unit test for MealsViewModel date switching
</automated>
</verify>
<acceptance_criteria>
- MealsViewModel can load data for any specific date.
- UI in MealsView updates when selectedDate changes.
</acceptance_criteria>
</task>

<task type="auto">
<read_first>
- LiiO_EatClean/Features/Progress/Components/CalendarHeatmapView.swift
</read_first>
<action>
Implement `DailyAdherenceDetailSheet.swift`:
- Display adherence score, status label, and macro progress.
- "Xem chi tiết Journal" button:
    - Sets `selectedTab = 1` in `ContentView`.
    - Updates `selectedDate` in `MealsViewModel`.
    - Dismisses the sheet.
</action>
<verify>
<automated>
# Manual interaction verification
</automated>
</verify>
<acceptance_criteria>
- Detail sheet displays correct data for the tapped day.
- Navigation button successfully switches to the Journal tab with the correct date selected.
</acceptance_criteria>
</task>

<task type="auto">
<read_first>
- LiiO_EatClean/Features/Progress/ProgressTabView.swift
</read_first>
<action>
Integrate `CalendarHeatmapView` into `ProgressTabView`:
- Add `.adherence` case to `ProgressTab` enum.
- Display `CalendarHeatmapView` when the tab is selected.
</action>
<verify>
<automated>
grep "CalendarHeatmapView" LiiO_EatClean/Features/Progress/ProgressTabView.swift
</automated>
</verify>
<acceptance_criteria>
- Heatmap is accessible via the Progress tab navigation.
</acceptance_criteria>
</task>
